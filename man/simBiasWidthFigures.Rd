% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulations.R
\name{simBiasWidthFigures}
\alias{simBiasWidthFigures}
\title{Bias / CI Width Ratio Figures Across CI Methods}
\usage{
simBiasWidthFigures(
  output.dirs = c(boot = "resiBootSim", normal = "resiAsympNormalSim", qf =
    "resiAsympQFSim"),
  figures.dir = NULL,
  alpha = 0.05
)
}
\arguments{
\item{output.dirs}{Named character vector mapping CI method labels to their
simulation output directories. Default:
\code{c(boot = "resiBootSim", normal = "resiAsympNormalSim", qf = "resiAsympQFSim")}.
Directories that do not exist are silently skipped.}

\item{figures.dir}{Character, directory where figures are saved. Default:
\code{file.path(output.dirs[[1]], "figures")}.}

\item{alpha}{Numeric, unused; kept for API consistency. Default 0.05.}
}
\value{
Invisibly returns the combined summary \code{data.frame}. Saves PDF
  figures named \code{sim_biaswidth_<model>_<vcov>.pdf} to \code{figures.dir}.
}
\description{
Reads simulation summary tables from multiple output directories (one per CI
method) and produces one PDF per (model type \eqn{\times} variance estimator)
combination. Each figure has one row per CI method showing \code{bias / CI width}
vs sample size for anova and coefficient tables. Values near \eqn{\pm 0.5}
indicate that bias alone can shift the CI off the true value.
}
\seealso{
\code{\link{simFigures}}, \code{\link{simCompareMethodsFigures}}
}
